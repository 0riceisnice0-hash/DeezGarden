---
// Compact grid-style "Why Choose Us" for mobile

const reasons = [
  {
    icon: "⭐",
    title: "Professional Service",
    description: "Experienced team delivering high-quality results every time with attention to detail."
  },
  {
    icon: "💰",
    title: "Fair Pricing",
    description: "Competitive rates with no hidden fees. Clear quotes upfront that we stick to."
  },
  {
    icon: "📋",
    title: "Licensed Waste Carrier",
    description: "All waste disposed of legally and responsibly in accordance with regulations."
  },
  {
    icon: "🛡️",
    title: "Fully Insured",
    description: "Complete peace of mind with full insurance coverage for all aspects of our work."
  }
];
---

<div class="why-choose-mobile md:hidden">
  <div class="grid grid-cols-2 gap-2 px-4">
    {reasons.map((reason) => (
      <div class="bg-bg-sage rounded-lg p-3 shadow-md border-l-4 border-fresh-green">
        <div class="text-2xl mb-1">{reason.icon}</div>
        <h4 class="font-extrabold text-sm text-deep-green mb-1">{reason.title}</h4>
        <p class="text-xs text-gray-600 leading-snug">{reason.description}</p>
      </div>
    ))}
  </div>
</div>

<!-- Desktop version -->
<div class="hidden md:block">
  <div class="space-y-5">
    {reasons.map((reason, index) => (
      <div class={`pl-4 border-l-4 ${index % 2 === 0 ? 'border-fresh-green' : 'border-deep-green'}`}>
        <h3 class="text-base font-extrabold mb-1">{reason.title}</h3>
        <p class="text-gray-700 text-sm leading-relaxed">{reason.description}</p>
      </div>
    ))}
  </div>
</div>
