---
import { siteConfig } from '../config';

interface Props {
  text?: string;
  variant?: 'primary' | 'secondary';
  size?: 'sm' | 'md' | 'lg';
}

const { 
  text = "Call Now for Free Quote", 
  variant = 'primary',
  size = 'lg' 
} = Astro.props;

const sizeClasses = {
  sm: 'px-4 py-2 text-sm',
  md: 'px-6 py-3 text-base',
  lg: 'px-8 py-4 text-lg'
};

const variantClasses = {
  primary: 'bg-fresh-green hover:bg-deep-green',
  secondary: 'bg-accent-yellow hover:bg-yellow-500 text-text-dark'
};
---

<div class="text-center my-8">
  <a 
    href={`tel:${siteConfig.phone}`}
    class={`inline-block text-white font-bold rounded-lg transition shadow-lg hover:shadow-xl ${variantClasses[variant]} ${sizeClasses[size]}`}
  >
    📞 {text}
  </a>
  <p class="mt-3 text-lg font-semibold text-deep-green">{siteConfig.phoneFormatted}</p>
</div>
